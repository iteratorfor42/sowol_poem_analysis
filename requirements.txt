# KPoEM 모델 구동에 필요한 핵심 의존성
# (OCR 프로젝트와 달리 이번엔 선택 사항이 아니라 필수 의존성입니다.
#  Python 3.14 등에서 torch 설치가 안 된다면, README의 "환경 관련 참고"
#  섹션을 확인해 Python 3.11~3.12 가상환경을 따로 만드는 것을 권장합니다.)
transformers>=4.35.0
torch>=2.0.0
huggingface_hub>=0.20.0

# 시각화
matplotlib>=3.8.0
numpy>=1.24.0

# 향후 확장: 형태소 분석 기반 문체 비교 등에 사용 가능 (현재 파이프라인엔 미사용)
# kiwipiepy>=0.17.0
